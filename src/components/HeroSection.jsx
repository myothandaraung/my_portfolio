import { ArrowDownRight, Github, Linkedin } from "lucide-react";
import { motion as Motion } from "framer-motion";
import { useTranslation } from "react-i18next";
import profileImage from "../assets/profile.png";
export default function HeroSection() { const { t } = useTranslation(); return <section id="top" className="hero section-wrap"><Motion.div className="hero-copy" initial={{ opacity: 0, y: 28 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: .7 }}><p className="eyebrow"><span /> Available for select projects</p><h1>Full-stack developer crafting <em>thoughtful digital experiences.</em></h1><p className="hero-intro">{t("hero.description")}</p><div className="hero-actions"><a className="button button--solid" href="#projects">Explore my work <ArrowDownRight size={18} /></a><a className="text-link" href="#contact">Start a conversation</a></div><div className="hero-meta"><span>Based in Myanmar</span><span>5+ years experience</span><div><a href="#"><Github size={17} /></a><a href="#"><Linkedin size={17} /></a></div></div></Motion.div><Motion.div className="portrait-wrap" initial={{ opacity: 0, scale: .96 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: .8, delay: .15 }}><div className="portrait-frame"><img src={profileImage} alt="Myo Thandar Aung" /></div><div className="portrait-note"><small>Currently</small><strong>Building useful things<br />for the web.</strong></div><span className="portrait-index">01 / 05</span></Motion.div></section>; }

